import React from 'react'
import ReactDOM from 'react-dom/client'
import LiveConfig from '../component/live_config/LiveConfig'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
	<React.StrictMode>
		<LiveConfig />
	</React.StrictMode>,
)